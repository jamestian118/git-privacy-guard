# CHANGELOG

All notable changes to this project will be documented in this file.

## 0.1.0 - 2026-02-27

### Added
- `pyproject.toml`, package entrypoint, and uninstall command.
- Test suite with coverage gate (`>=60%`).
- CI workflow (`ruff` + verify/pytest).

### Changed
- Config validation and custom pattern support in guard logic.
- README uninstall/init flag docs (`--force`, `--no-gitignore`) and path parameterization examples.
